name: CI

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint markdown files
        run: |
          npx --yes markdownlint-cli2 "skills/astro-best-practices/rules/**/*.mdc" "README.md" "CONTRIBUTING.md" "CODE_OF_CONDUCT.md" "CHANGELOG.md" "SECURITY.md" "skills/astro-best-practices/SKILL.md" "skills/astro-best-practices/test-cases/**/*.md" "skills/astro-best-practices/eval/*.md" || true
      - name: Check SKILL.md frontmatter
        run: |
          if ! grep -q '^name:' skills/astro-best-practices/SKILL.md; then echo "SKILL.md missing 'name' field"; exit 1; fi
          if ! grep -q '^description:' skills/astro-best-practices/SKILL.md; then echo "SKILL.md missing 'description' field"; exit 1; fi

  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate YAML
        run: |
          npx --yes js-yaml skills/astro-best-practices/eval/quantitative-metrics.yaml > /dev/null && echo "YAML valid" || { echo "YAML invalid"; exit 1; }
      - name: Check test cases structure
        run: |
          test -d skills/astro-best-practices/test-cases/trigger && echo "trigger dir OK"
          test -d skills/astro-best-practices/test-cases/non-trigger && echo "non-trigger dir OK"
          test -d skills/astro-best-practices/rules && echo "rules dir OK"
          test -d skills/astro-best-practices/snippets && echo "snippets dir OK"
